# Local-only architectural notes, not for public README consumption
CLAUDE.md

# Opt-in emoji list generated by install.sh --emoji (user-fetched)
emoji_full.txt

# Codex skill state (pane ID, session ID, transient prompts)
.context/
